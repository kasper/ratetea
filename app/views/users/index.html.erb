<h1>Listing users</h1>

<% unless @users.empty? %>
  <ul>
    <% @users.each do |user| %>
      <li><%= link_to user.username, user %></li>
    <% end %>
  </ul>
<% end %>

<%= link_to 'New User', new_user_path %>
