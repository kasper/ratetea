<%= form_for(@tea_club) do |f| %>
  <% if @tea_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tea_club.errors.count, "error") %> prohibited this tea_club from being saved:</h2>

      <ul>
      <% @tea_club.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :founded %><br>
    <%= f.number_field :founded %>
  </div>
  <div class="field">
    <%= f.label :city %><br>
    <%= f.text_field :city %>
  </div>
  <div class="actions">
    <%= f.submit :class => 'btn' %>
  </div>
<% end %>
